<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical" >
    <TextView 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="20dp"
        android:text="填写手机号码，可以更快获得律师回复。"
        android:textSize="@dimen/tv_style_smal_size"
        android:textColor="@color/text_light_color"
        />
    <EditText 
        android:id="@+id/regist_phone_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
       	android:drawableLeft="@drawable/user_icon"
       	android:drawableStart="@drawable/user_icon"
       	android:drawablePadding="5dp"
       	android:gravity="center_vertical"
       	android:textCursorDrawable="@null"
       	android:singleLine="true"
       	android:maxLength="11"
       	android:inputType="number"
        android:hint="手机号"/>
    
    <RelativeLayout 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp">
        <Button 
            android:id="@+id/repeate_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/code_button_style"
            android:layout_alignParentRight="true"
            android:padding="5dp"
            android:onClick="doCode"
            android:text="获取验证码"/>
        <EditText 
            android:id="@+id/regist_test_code"
	        android:layout_width="match_parent"
	        android:layout_height="wrap_content"
	        android:layout_toLeftOf="@id/repeate_send"
	        android:layout_marginRight="15dp"
	        android:textCursorDrawable="@null"
       		android:singleLine="true"
	       	android:drawableLeft="@drawable/pswd_icon"
	       	android:drawableStart="@drawable/pswd_icon"
	       	android:drawablePadding="5dp"
	       	android:gravity="center_vertical"
	       	android:maxLength="6"
       		android:inputType="number"
	        android:hint="验证码"/>
    </RelativeLayout>
    <Button 
        android:id="@+id/send"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:visibility="gone"
        android:text="登录"/>
</LinearLayout>